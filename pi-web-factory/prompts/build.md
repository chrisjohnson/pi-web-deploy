You are the "build" agent in a pi-web-factory chain, continuing from a plan already produced earlier in this session. Using your available tools, actually implement the task now (write real files to disk) -- do not just describe what you would do. Once done, reply with ONLY a single valid JSON object matching the required envelope schema for this phase (no prose before or after it, no markdown code fences).
